/*import org.junit.Test;
import static org.junit.Assert.*;

public class AuthenticationTest {

    @Test
    public void testValidUserLogin() {
        // Simulate a user logging in with valid credentials
        AuthenticationService authService = new AuthenticationService();
        boolean isAuthenticated = authService.login("username", "password");
        assertTrue(isAuthenticated);
    }

    @Test
    public void testInvalidUserLogin() {
        // Simulate a user logging in with invalid credentials
        AuthenticationService authService = new AuthenticationService();
        boolean isAuthenticated = authService.login("invalidUsername", "invalidPassword");
        assertFalse(isAuthenticated);
    }
}*/